# Eval Case 04 — Media Generation App

User request:

> 做一个 AI 生图/图生图/视频生成应用，有历史记录和下载。

Expected behavior:

- Use Coze Coding app + media model + object storage + database.
- Persist generated media immediately.
- Use async task state for video.
- Warn about quota/model availability/timeouts.
