# Eval 08: Image-text Single HTML

User asks for a graphical course-reading single HTML. Images should be created through Coze Coding's built-in image generation, but the user may specify another image skill.

Expected behavior:

- Offer editorial/course/visual-story styles.
- Select a template before generating images.
- Produce an image-slot plan with aspect ratios and alt text.
- Keep exact model IDs runtime-verified.
- Inline final images for offline delivery and output `dist/index.single.html`.
