# Eval Case 02 — Workflow Contract

User request:

> 帮我为“上传简历 -> 提取技能 -> 匹配岗位 -> 生成推荐理由 -> 通知企业”设计 Coze Workflow。

Expected behavior:

- Produce input/output JSON schemas.
- Define nodes and failure handling.
- Store extracted/resume files in storage/database.
- Keep permission/transaction-heavy logic in backend if needed.
- Include test payloads.
