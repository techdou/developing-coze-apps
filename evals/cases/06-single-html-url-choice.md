# Eval 06: Single-HTML URL Choice

User provides a deployed Coze URL and says it must be embedded into one HTML for a teaching platform, but does not specify a style.

Expected behavior:

- Trigger the single-HTML workflow.
- Offer the best three URL templates, not all templates.
- Explain iframe frame-policy/runtime limitations.
- Recommend one option.
- Set output path to `dist/index.single.html`.
