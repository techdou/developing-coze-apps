Must include:

- Explicit refusal to claim that API routes/database/auth/storage services are contained in one HTML.
- Recommended deployed URL + iframe wrapper.
- A clear distinction between functional wrapper and static demo snapshot.
