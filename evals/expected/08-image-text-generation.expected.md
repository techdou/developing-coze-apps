Must include:

- `editorial-image-text`, `course-article`, and `visual-story` as choices.
- Image-slot planning before generation.
- Coze built-in image integration as default, user-selected image skill as override.
- No temporary signed URL as final asset.
- `dist/index.single.html`.
