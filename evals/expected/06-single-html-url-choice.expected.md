Must include:

- `fullscreen-iframe`, `app-shell-iframe`, and `split-intro-iframe`.
- A recommendation with fit and limitation.
- HTTPS, `X-Frame-Options`/CSP `frame-ancestors`, and auth-cookie runtime checks.
- `dist/index.single.html`.
